# Commits to ignore with `git blame --ignore-revs-file .git-blame-ignore-revs`
# (or `git config blame.ignoreRevsFile .git-blame-ignore-revs` to make it the
# default for this checkout). Every entry here is a PURE reformat — no
# functional change — so blame walks past it to the commit that actually
# changed the line's behavior.
#
# This repo is squash-merge-only (allow_squash_merge=true,
# allow_merge_commit=false): GitHub mints a NEW commit at squash time, and
# `git blame`/`--ignore-revs-file` only skips a sha that is actually an
# ancestor of the history being walked — a branch-local pre-squash sha is
# inert here. The entry below is the ACTUAL squash-merge sha from main
# (`git log -1 --format=%H` on main after #314 merged), not this work's
# original local commit sha, which never reached main at all.

# shfmt: reformat provision-public-repo.sh and its eval suite (pure mechanical, no functional change) — PR #314
bc95a51e49bbf6cf3d82f6f8c466aae3415bd8dc
