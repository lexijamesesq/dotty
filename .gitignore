.DS_Store
.github-prep-status.json

# Private config — use the .sample files as templates
CLAUDE.md

# .claude/settings.json is committed (LEX-702): repo-scope plugin/marketplace
# declaration a fresh clone needs to rebuild its harness alone.

# Python bytecode cache
__pycache__/
*.pyc
.claude/evals/

# Local vendored-reference working directory — never ships (operator ruling)
reference/
