.DS_Store
.github-prep-status.json

# Private config — use the .sample files as templates
CLAUDE.md
settings.json

# update-mbp: real exclusions.txt reflects this machine's personal picks —
# gitignored like CLAUDE.md/settings.json above; exclusions.sample.txt ships as the template
.claude/skills/update-mbp/exclusions.txt

# Operator PII rules (symlink -> ~/bin/dotty-private/gitleaks-operator-rules.toml)
.gitleaks-operator-rules.toml

# Python bytecode cache
__pycache__/
*.pyc
.claude/evals/

# Local vendored-reference working directory — never ships (operator ruling)
reference/
