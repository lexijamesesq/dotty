{
  "_comment": "Sample settings.json for public consumers of dotty's plugin-install harness — this is the estate's own full session shape (env, permissions, statusline), not dotty's own project-scope declaration (that's the real, committed .claude/settings.json alongside this file, which only declares the plugins a session opened in dotty needs — see LEX-702). Copy the blocks you want into your own settings.json. Hooks and the statusline are NOT registered by path here — they ship inside the enabled plugins (installed and enabled separately, e.g. via `claude plugin marketplace add` / `install` / `enable`) and the harness installs the statusline to a fixed path. TODO: set VAULT_ROOT to your Obsidian vault (the vault-aware hooks fail-open and silently disable themselves without it), and swap core/estate-hooks/publish for your own marketplace and plugin names if you're not using this estate's.",
  "env": {
    "VAULT_ROOT": "path/to/your/obsidian-vault"
  },
  "permissions": {
    "allow": [
      "Task",
      "WebFetch",
      "WebSearch"
    ],
    "ask": [
      "Bash(git push)",
      "Bash(git push *)",
      "Bash(git -C * push)",
      "Bash(git -C * push *)",
      "Bash(gh pr create)",
      "Bash(gh pr create *)",
      "Bash(gh pr merge)",
      "Bash(gh pr merge *)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "~/.config/claude-estate/statusline.sh"
  },
  "enabledPlugins": {
    "core@core": true,
    "estate-hooks@core": true,
    "publish@publish": true
  },
  "hooks": {},
  "extraKnownMarketplaces": {
    "core": {
      "source": {
        "repo": "lexijamesesq/core-skills",
        "source": "github"
      }
    },
    "publish": {
      "source": {
        "repo": "lexijamesesq/publish-skills",
        "source": "github"
      }
    }
  }
}
